#include "TAB.c"
/*
void colore(TAB *a){
    if (!a) return;
    a->cor = 1;
    col_aux(a,!a->cor);
}

void col_aux(TAB *a, int cor){
    if (!a) return;
    if (a->esq){
        a->esq->cor = cor;
        col_aux(a->esq,!a->esq->cor);
    }
    if (a->dir){
        a->dir->cor=cor;
        col_aux(a->dir,!a->dir->cor);
    }
}
*/